<Page
    x:Class="AttendanceTracker.Views.World2"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AttendanceTracker.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        
        <!-- SplitView for all the Pages Starts-->
        
        <SplitView x:ConnectionId='1' x:Name="MySplitView" DisplayMode="CompactOverlay"  IsPaneOpen="False" 
               CompactPaneLength="50" OpenPaneLength="180">
            <SplitView.Pane>
                <StackPanel Background="Gray">
                    <Button x:ConnectionId='3' x:Name="HamburgerButton" FontFamily="Segoe MDL2 Assets" Content="&#xE700;"
                    Width="50" Height="50" Background="Transparent"                              />
                    <StackPanel x:ConnectionId='4'  x:Name="MenuStack1" Orientation="Horizontal">
                        <Button FontFamily="Segoe MDL2 Assets" Content="&#xE825;"
                   Width="50" Height="50" Background="Transparent"/>
                        <Button x:ConnectionId='9' x:Name="MenuButton11" Content="Home" Height="50" HorizontalAlignment="Stretch" Background="Transparent"                           FontSize="18"></Button>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <Button x:ConnectionId='7' x:Name="MenuButton2" FontFamily="Segoe MDL2 Assets" Content="&#xE10F;"
                       Width="50" Height="50" Background="Transparent"/>
                        <Button x:ConnectionId='8' x:Name="MenuButton22" Content="TimeTable" Height="50" HorizontalAlignment="Stretch" Background="Transparent"                           FontSize="18"></Button>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <Button x:ConnectionId='5' x:Name="MenuButton3" FontFamily="Segoe MDL2 Assets" Content="&#xE1D6;"
                       Width="50" Height="50" Background="Transparent"                          />
                        <Button x:ConnectionId='6' x:Name="MenuButton33" Content="Setting" Height="50" HorizontalAlignment="Stretch" Background="Transparent"                           FontSize="18"></Button>
                    </StackPanel>
                </StackPanel>
            </SplitView.Pane>
        </SplitView>

        <!-- SplitView for all the Pages, Ends -->

    <!-- Main Home of the contents -->
        <GridView x:ConnectionId='2' x:Name="GridViewOfSubjects">

            
            
            <!--Item Templates-->
            <GridView.ItemTemplate>
                <DataTemplate>
                    
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>

        <!-- Main home ends -->
    </Grid>
</Page>

